import 'package:flutter/material.dart';

/// Primary color used in the app.
const kPrimaryColor = Color(0xFF6F35A5);

/// Lighter shade of the primary color.
const kPrimaryLightColor = Color(0XFFF1E6FF);

/// Red color used for todo items.
const Color tdRed = Color(0xFFDA4040);

/// Blue color used for todo items.
const Color tdBlue = Color(0xFF5F52EE);

/// Black color used for text and icons.
const Color tdBlack = Color(0xFF3A3A3A);

/// Grey color used for secondary text and icons.
const Color tdGrey = Color(0xFF717171);

/// Background color used in the app.
const Color tdBGColor = Color(0xFFEEEFF5);
